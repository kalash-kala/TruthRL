## VERL

This is a lightweight placeholder README for the local `verl` package used by TruthRL.

It exists to satisfy packaging metadata (`pyproject.toml` and `setup.py`) that expect a `README.md`
in the project root when building or installing the package (including editable installs).

